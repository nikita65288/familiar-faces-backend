server:
  port: 8761

spring:
  application:
    name: discovery-service
  profiles:
    active: local

eureka:
  client:
    register-with-eureka: false # Do not register itself
    fetch-registry: false